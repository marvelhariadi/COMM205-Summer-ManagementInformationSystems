my_courses <- c("COMM 205", "COMM 291", "COMM 395")

my_courses[4:5] <- c("COMM 204", "ENGL 112") #adds new data elements into index 4 & 5

length(my_courses)

#---
  
x=  
x !== 7 & first_name == "Fred" & last_name == "Smith{"

#---
  
 set.seed(888)
rand_vec<-runif(1000000,min=0,max=100)
sd(rand_vec)

---
my_cars <- mtcars
length(mtcars)
length(mtcars$mpg)
min(mtcars$mpg)
sum(mtcars$hp)/length(mtcars$hp)